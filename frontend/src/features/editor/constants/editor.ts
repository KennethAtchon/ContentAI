/** Debounce for PATCH autosave (EditorLayout + useEditorProject). */
export const EDITOR_AUTOSAVE_DEBOUNCE_MS = 2000;

/** Periodic heartbeat save interval — fires even if no changes detected. */
export const EDITOR_AUTOSAVE_INTERVAL_MS = 30_000;
