This will be where you describe the user journey. Here we will let you know these items:

- Everything the user sees for a page
- What buttons the user can click to go places and where
- What the user can DO 
- basically the entire UI/UX experience writtened out.

TODOs:

The UI/UX is pretty jarring between pages, pages got their own unique layout that - works for them, but when you navigate away its a pretty jarring change. We need a way to fix it. 