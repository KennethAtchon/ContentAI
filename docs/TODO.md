Content Queue

- WE need to remove the "add to queue" functionality, it should be automatically added, queue is only needed to manage your stuff at scale and know when something is ready behind the scene. Jobs should never stop until completed or failure and event is send to the frontend to let us know.

- Content Queue, shows status of EVERYTHING, video failed, voice over failed, etc.

- Move it through the flow in content queue. Make edit button open up a UI that displays everything in generatedcontent. Content queue should be the tools users use for visualibility, so make sure you include all the information there.

We need to make content queue hands free (like a pipeline that AI OR manual editor updates). And make the edit and duplicate buttons actually work.