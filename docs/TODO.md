Add uploading video files to the chat messages, and we can process it (should use the same mechanism as adding reels)

Let users know the AIs capabilities like reading images, videos, etc

Make editor studio page actually work. Allow it to pick up artifacts from generated project (make it scoped to one generated content project at a time) and allow us to perform on that.

Let users have a media library that we can pull from and they can upload to for use in the editor or generate. Add the media library to the editor.

Let users have 1 editor project per generated content so they aren't editting multiple contents at the same time.

Let users *publish* reels, which means it can't be editted anymore and they have to create a new draft, this serves as a control. 

I'm spinning off ideas to improve user ability and actually make this a really good application for generating reels on the fly.

Advanced work:

Study assembly of reels from shots and create a perfect assembly system with the editor.
Once the assembly system with the editor is perfected, add the integration with AI, so AI can assemble for users.

