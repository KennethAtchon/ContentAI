When we click the start reel generation button. So we wait for generating reel right, the little toast that comes up is GOOD, but theres no way to like, minimize it? because it hides other part of the UI. And also looks like they dont dismiss at all, even when done, and when its ready it still tells you that it will take a few minutes.

