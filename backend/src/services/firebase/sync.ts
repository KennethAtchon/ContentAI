/**
 * Re-export — implementation lives in `domain/auth/firebase-user-sync.ts`.
 * @deprecated Prefer importing from `domain/auth/firebase-user-sync`.
 */
export {
  FirebaseUserSync,
  type SyncResult,
} from "../../domain/auth/firebase-user-sync";
